<!doctype html>
<html lang="ru-RU">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ ln.install_title }}</title>
    <link rel="stylesheet" href="/lib/bootstrap/css/bootstrap.css"/>
    <link rel="stylesheet" href="/lib/bootstrap/css/bootstrap-responsive.css"/>
    <link rel="stylesheet" href="/install/components/builder/assets/css/main.css"/>
    <style type="text/css">
        <style type="text/css">
        body { padding-top: 40px; }
    </style>
    </style>
</head>
<body>
<div class="container">
    {{ messages }}
    <div class="page-header">
        <h1>{{ ln.install_params_step3 }} <small>{{ ln.install_params_step3_desc }}</small></h1>
    </div>
    <form class="form-horizontal" action="/install/" method="GET">
        {% for field in fields %}
            <div class="control-group{% if field.error %} error{% endif %}">
                <label class="control-label" for="{{ field.name }}">{{ field.title }} {% if field.required %}<span class="red">*</span>{% endif %}</label>
                <div class="controls">
                    {{ field.html }}
                    <p class="muted"><small>{{ field.description }}</small></p>
                </div>
            </div>
        {% endfor %}
        <div class="control-group">
            <div class="controls">
                <input type="hidden" name="task" value="setParams" />
                <button type="submit" name="view" value="params" class="btn btn-info">{{ ln.install_params_continue }}</button>
            </div>
        </div>
    </form>
</div>
</body>
</html>
