<!doctype html>
<html lang="ru-RU">
<head>
    <meta charset="UTF-8">
    <title>Мастер установки SamCMS</title>
    <link rel="stylesheet" href="/lib/bootstrap/css/bootstrap.css"/>
    <link rel="stylesheet" href="/lib/bootstrap/css/bootstrap-responsive.css"/>
    <link rel="stylesheet" href="/install/components/builder/assets/css/main.css"/>
</head>
<body>
    <div class="container">
        <div class="hero-unit text-center">
            <h1>Добро пожаловать</h1>
            <p>в мастер установки SamCMS</p>
        </div>
        <div class="row">
            <div class="span5 offset2">
                <p>Чтобы начать пользоваться системой управлением контентом SamCMS, необходимо установить ее на ваш сервер. Для этого пройдите несколько простых шагов установки.</p>
                <p>Перед началом установки выберите язык установки.</p>
            </div>
            {% if languages %}
            <div class="span5">
                <p>
                    <label for="language">Выберите язык установки:</label>
                    <select name="language" id="language" multiple>
                        {% for language in languages %}
                        <option value="{{ languae.value }}">{{ language.title }}</option>
                        {% endfor %}
                    </select>
                </p>
            </div>
            {% endif %}
        </div>
        <div class="row text-center">
            <p><button class="btn btn-large btn-primary">Продолжить</button></p>
        </div>
    </div>
</body>
</html>