<div>
    <h1>{{ ln.menueditor_items }} <small>{{ menu.title }}</small></h1>
    <form action="{{ url }}" method="POST">
        <div class="buttons">
            <div class="btn-group">
                <a href="{{ back }}" class="btn btn-default"><i class="glyphicon glyphicon-chevron-left"></i>&nbsp;{{ ln.menueditor_back }}</a>
                {% if types %}
                <button type="button" class="btn btn-default dropdown-toggle" data-toggle="dropdown"><i class="glyphicon glyphicon-plus"></i>&nbsp;{{ ln.menueditor_add }} <span class="caret"></span></button>
                <ul class="dropdown-menu">
                    {% for type in types %}
                    <li><a href="{{ type.url }}">{{ type.title }}</a></li>
                    {% endfor %}
                </ul>
                {% endif %}
                <button name="task" value="delete" type="submit" class="btn btn-default"><i class="glyphicon glyphicon-trash"></i>&nbsp;{{ ln.menueditor_delete }}</button>
            </div>
            <div style="clear: both;"></div>
        </div>
        <div class="menus">
            <table class="table table-striped">
                <tr>
                    <th class="th30"><input type="checkbox" class="check-all"></th>
                    <th class="th30">ID</th>
                    <th>{{ ln.menueditor_name }}</th>
                    <th>{{ ln.menueditor_component }}</th>
                    <th>{{ ln.menueditor_alias }}</th>
                    <th>{{ ln.menueditor_active }}</th>
                    <th>{{ ln.menueditor_visible }}</th>
                    <th class="th80"></th>
                    <th class="th100"></th>
                </tr>
                {% for item in items %}
                <tr>
                    <td><input type="checkbox" class="check-item" name="item[]" value="{{ item.id }}"></td>
                    <td>{{ item.id }}</td>
                    <td><a href="{{ item.edit }}">{{ item.title }}</a></td>
                    <td>
                        {% if item.component_title %}
                        <span class="label label-info">{{ item.component_title }}</span>
                        {% endif %}
                    </td>
                    <td>{{ item.alias }}</td>
                    <td>
                        {% if item.active %}
                        <i class="glyphicon glyphicon-ok"></i>
                        {% else %}
                        <i class="glyphicon glyphicon-remove"></i>
                        {% endif %}
                    </td>
                    <td>
                        {% if item.visible %}
                        <i class="glyphicon glyphicon-ok"></i>
                        {% else %}
                        <i class="glyphicon glyphicon-remove"></i>
                        {% endif %}
                    </td>
                    <td class="btnset">
                        <a href="{{ item.edit }}" title="{{ ln.menueditor_edit }}" type="button" class="btn btn-default"><i class="glyphicon glyphicon-pencil"></i></a>
                    </td>
                    <td class="btnset">
                        <div class="btn-group">
                            <button name="moveup" value="{{ item.id }}" class="btn btn-default {% if item.disabledUp %}disabled{% endif %}" {% if item.disabledUp %}disabled="disabled"{% endif %} title="{{ ln.menueditor_up }}"><i class="glyphicon glyphicon-chevron-up"></i></button>
                            <button name="movedown" value="{{ item.id }}" class="btn btn-default {% if item.disabledDown %}disabled{% endif %}" {% if item.disabledDown %}disabled="disabled"{% endif %} title="{{ ln.menueditor_down }}"><i class="glyphicon glyphicon-chevron-down"></i></button>
                        </div>
                    </td>
                </tr>
                {% endfor %}
            </table>
        </div>
    </form>
</div>