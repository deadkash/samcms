<div class="b-formbox">
    {% if result %}
    {% if result == 'error' %}
    <div class="alert fade in alert-danger">
        <button class="close" data-dismiss="alert">×</button>
        {{ ln.auth_invalid }}
    </div>
    {% endif %}
    {% endif %}
    <form class="well form-inline" method="POST" action="">
        <input name="login" type="text" class="input-medium" placeholder="{{ ln.auth_login }}">
        <input name="password" type="password" class="input-medium" placeholder="{{ ln.auth_password }}">
        <button type="submit" name="action" value="login" class="btn">{{ ln.auth_enter }}</button>
    </form>
    <div>
        <a href="{{ recover }}">{{ ln.auth_recover }}</a>
    </div>
</div>